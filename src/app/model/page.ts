export class Page {
    public number: number;
    public imageUrl: string;

    constructor(number: number, imageUrl: string) {
        this.number = number;
        this.imageUrl = imageUrl;
    }
}
